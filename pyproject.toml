[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "medi-guide-ai"
version = "0.1.0"
description = "AI Medical Guide Deployment"
dependencies = [
    "gradio",
]

# No server entry point - HF Spaces handles this
